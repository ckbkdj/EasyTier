diff --git a/easytier-core/src/socket/udp/layer.rs b/easytier-core/src/socket/udp/layer.rs
--- a/easytier-core/src/socket/udp/layer.rs
+++ b/easytier-core/src/socket/udp/layer.rs
@@ -157,6 +157,7 @@ where
             self.socket.clone(),
             local_addr,
             remote_addr,
+            UdpSocketSendMeta::default(),
             protocol.session_kind(),
             UdpSessionCodec::Identity,
             rings,
@@ -231,6 +232,7 @@ where
                     self.socket.clone(),
                     local_addr,
                     key.peer_addr,
+                    UdpSocketSendMeta::default(),
                     UdpSessionKind::EasyTierMux,
                     UdpSessionCodec::EasyTierData { conn_id },
                     rings,
@@ -574,6 +576,7 @@ where
             remote_addr,
             conn_id,
             &packet,
+            recv_meta,
             session_shutdown.subscribe(),
         ),
         EasyTierUdpPacketKind::Sack => {
@@ -660,6 +663,10 @@ where
     S: VirtualUdpSocket,
 {
     let key = ClassifiedUdpSessionKey::new(protocol, remote_addr);
+    let send_meta = UdpSocketSendMeta {
+        src_ip: datagram.dst_ip,
+        src_ifindex: None,
+    };
     if let Some(entry) = classified_sessions.get(&key) {
         let dispatched = dispatch_payload_to_session(
             &entry.incoming,
@@ -735,6 +742,7 @@ where
         socket,
         local_addr,
         remote_addr,
+        send_meta,
         protocol.session_kind(),
         UdpSessionCodec::Identity,
         rings,
@@ -754,6 +762,7 @@ pub(super) fn handle_new_easy_tier_mux_connect<S>(
     remote_addr: SocketAddr,
     conn_id: u32,
     packet: &ZCPacket,
+    recv_meta: UdpSocketRecvMeta,
     session_shutdown: watch::Receiver<bool>,
 ) -> bool
 where
@@ -771,10 +780,14 @@ where
     let magic = u64::from_le_bytes(payload[..8].try_into().unwrap());
     let key = UdpSessionKey::new(remote_addr, conn_id);
     let sack_packet = new_sack_packet(conn_id, magic).into_bytes();
+    let send_meta = UdpSocketSendMeta {
+        src_ip: recv_meta.dst_ip,
+        src_ifindex: None,
+    };
     if sessions.contains_key(&key) {
         let sessions = sessions.clone();
         tokio::spawn(async move {
-            if let Err(err) = socket.send_to(&sack_packet, remote_addr).await {
+            if let Err(err) = socket
+                .send_to_with_meta(&sack_packet, remote_addr, send_meta)
+                .await
+            {
                 tracing::debug!(?err, ?key, "udp resend sack packet error");
                 close_udp_session(&sessions, key);
             }
@@ -806,6 +819,7 @@ where
         socket.clone(),
         local_addr,
         key.peer_addr,
+        send_meta,
         UdpSessionKind::EasyTierMux,
         UdpSessionCodec::EasyTierData { conn_id },
         rings,
@@ -814,7 +828,10 @@ where
         session_shutdown,
     );
     tokio::spawn(async move {
-        if let Err(err) = socket.send_to(&sack_packet, remote_addr).await {
+        if let Err(err) = socket
+            .send_to_with_meta(&sack_packet, remote_addr, send_meta)
+            .await
+        {
             close_udp_session(&sessions, key);
             tracing::debug!(?err, ?key, "udp send sack packet error");
             return;
diff --git a/easytier-core/src/socket/udp/session.rs b/easytier-core/src/socket/udp/session.rs
--- a/easytier-core/src/socket/udp/session.rs
+++ b/easytier-core/src/socket/udp/session.rs
@@ -20,7 +20,10 @@ use super::{
     MAX_UDP_SESSION_DATAGRAM_SIZE, UDP_SESSION_QUEUE_CAPACITY,
     packet::{new_data_packet, udp_session_payload_len},
-    virtual_socket::{PreferredIpv6Source, UdpBindOptions, UdpSocketRecvMeta, VirtualUdpSocket},
+    virtual_socket::{
+        PreferredIpv6Source, UdpBindOptions, UdpSocketRecvMeta, UdpSocketSendMeta,
+        VirtualUdpSocket,
+    },
 };
@@ -406,6 +409,7 @@ impl UdpSession {
         socket: Arc<S>,
         local_addr: SocketAddr,
         peer_addr: SocketAddr,
+        send_meta: UdpSocketSendMeta,
         kind: UdpSessionKind,
         codec: UdpSessionCodec,
         rings: UdpSessionRingParts,
@@ -421,6 +425,7 @@ impl UdpSession {
         let send_task = tokio::spawn(forward_udp_session_to_socket(
             socket,
             peer_addr,
+            send_meta,
             codec,
             rings.session_send_rx,
             close.clone(),
@@ -710,6 +715,7 @@ async fn forward_udp_session_to_socket<S>(
     socket: Arc<S>,
     peer_addr: SocketAddr,
+    send_meta: UdpSocketSendMeta,
     codec: UdpSessionCodec,
     mut outgoing: RingSocketReceiver<UdpSessionOutbound>,
     close: UdpSessionClose,
@@ -756,7 +762,10 @@ async fn forward_udp_session_to_socket<S>(
                 (datagram, None)
             }
         };
-        match socket.send_to(&datagram, peer_addr).await {
+        match socket
+            .send_to_with_meta(&datagram, peer_addr, send_meta)
+            .await
+        {
             Ok(_) => {
                 if let Some((completion, payload_len)) = completion {
                     let _ = completion.send(Ok(payload_len));
@@ -832,6 +841,7 @@ mod tests {
                 socket.clone(),
                 local_addr,
                 peer_addr,
+                UdpSocketSendMeta::default(),
                 kind,
                 UdpSessionCodec::Identity,
                 rings,
